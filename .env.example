# 复制为 .env 后按需修改

# 对外访问端口(浏览器访问 http://服务器IP:HTTP_PORT)
HTTP_PORT=8080

# PostgreSQL 凭据(生产环境务必修改密码)
POSTGRES_USER=finance
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=finance

# 允许的跨域来源,* 表示不限制(同源部署可保持 *)
CORS_ORIGINS=*
