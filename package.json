{
  "name": "cw-workspace",
  "private": true,
  "description": "小企业财务记账系统 —— 仓库级脚本(共享契约同步)。Web 前端与小程序各自独立安装依赖。",
  "scripts": {
    "shared:sync": "node scripts/sync-shared.mjs",
    "shared:check": "node scripts/sync-shared.mjs --check"
  }
}
